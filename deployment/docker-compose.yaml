name: quickannotator_services

services:
  postgis:
    image: postgis/postgis:latest@sha256:d17234f17ac33553ac15fbe6d1040648f71c79eafa85fbc0fe2f82c89cb1ca17
    container_name: qa_postgis
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: qa_postgis_db
    ports:
      - "5432:5432"
    volumes:
      - postgis_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - qa_net

  grafana:
    image: grafana/grafana:latest@sha256:62d2b9d20a19714ebfe48d1bb405086081bc602aa053e28cf6d73c7537640dfb
    container_name: qa_grafana
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
    ports:
      - "3000:3000"
    depends_on:
      - postgis
    restart: unless-stopped
    networks:
      - qa_net

  qa:
    image: histotools/quickannotator:main
    container_name: qa
    volumes:
      - nas_read:/opt/QuickAnnotator/quickannotator/mounts/nas_read
      - nas_write:/opt/QuickAnnotator/quickannotator/mounts/nas_write
    networks:
      - qa_net
    depends_on:
      - postgis
    environment:
      - POSTGRES_HOST=qa_postgis
    ports:
      - "5000:5000"
      - "5173:5173"
      - "8265:8265"
      - "6006:6006"
    command: /bin/bash
    stdin_open: true
    tty: true
    shm_size: '12gb'
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]


networks:
  qa_net:
    driver: bridge

volumes:
  postgis_data:
  nas_read:
  nas_write:
  # qadb_data: # only needed if using sqlite db